# Cognitive Terrain — 发布与改善计划

- 会话溯源：`019fc667-4032-75f7-8906-a9e1de2f8df4`（pi 会话，2026-08-03）
- 项目：`/Users/qianyuhe/Documents/GitHub/cognitive-terrain`
- 目标：从"完成度很高的单机 demo / 研究原型"推进到"可发布、可持续经营的知识地图产品"
- 结论先行：**当前整体"可当产品卖/周活"约 40%**；视觉 75–85%、核心算法骨架 70%，短板在工作流（25–35%）、数据可信（30–40%）、工程交付（20–30%）

---

## 现状基线（上线前必须稳住，不允许退回）

- `npm run typecheck` → 0
- `npm run lint` → 0
- `npm test` → 10/10（unit，默认 runners 里有 e2e / visual / a11y script，但 tests 目录基本只有 unit）
- `npm run build` → 成功（chunk >500kB 警告，预期）
- 活验通过：时间轴拖拽（2025年12月 ↔ 2022年5月）、播放/暂停、控制台无错误
- 截图：`output/playwright/resume-desktop.png` / `resume-mobile.png`

### 已知风险（发布前处理）

| 风险 | 说明 | 处理 |
| --- | --- | --- |
| 仓库形态 | 在 monorepo 脏工作区里，目录 untracked，未独立提交 | 收成独立仓库 + 首次提交（见 P0-1） |
| bundle 体积 | 主包 ~1.4MB + ORT wasm ~23MB | P0-5 拆包 |
| 沉默降级 | 模型失败静默落到 hash 向量 | P0-2 禁默默装懂 |

---

## P0（发布阻塞项，按序执行）

### P0-1 工程与分发形态
- [ ] 将 `cognitive-terrain/` 收成独立 git 仓库，写清 `.gitignore`（node_modules/dist/本地数据）
- [ ] 版本号落地：`package.json` 显式版本（当前 0.1.0）、`git tag`
- [ ] CI：typecheck + lint + `npm test` + build 四道门，任一失败即红
- [ ] 可部署 URL（Vercel / Cloudflare Pages / GH Pages），README 附线上链接

### P0-2 语义可信度（禁止默默装懂）
- [ ] 界面明确标注当前模式徽章：**语义模式** vs **降级模式**（deterministic hash）
- [ ] "加载今日学习"不再强制 deterministic；正式学习包走真 embedding，失败才降级并提示
- [ ] 选中笔记显示 top-k 近邻（共同标签 / 关键词），交代"为什么挨在一起"
- [ ] 分析失败原因、降级率、导入耗时入口可观测（控制台面板即可，不必上后端）

### P0-3 数据生命周期（复用已有存储层）
- [ ] 项目库 UI：列表 / 切换 / 重命名 / 删除（`listProjectSummaries` / `deleteProject` 已存在）
- [ ] 多版本/快照对比：同一知识库不同月份并排
- [ ] 自动备份提醒 + 一键导出完整包（`.terrain.json` 已有）；清站点数据前保护
- [ ] 保存视图：搜索 + 筛选组合可命名存下来（如"只看 Agent Harness + 2026"）

### P0-4 分享物（面向发布/传播）
- [ ] 分享卡片：地图 + 时间范围 + 顶峰主题 + 笔记数，专为发 X / 即刻设计
- [ ] PNG 导出包含叙事层（品牌、时间、主题），不只是切画布
- [ ] 真实数据接入：用"我的笔记"演示，替换 1080 条合成 demo 作为默认推荐项

### P0-5 体积与首屏
- [ ] Three / transformers 按需拆包（动态 import）
- [ ] demo 模式不下载大模型；首屏可交互预算明确
- [ ] README 安全审计（现有 4 high）整改：供应链策略、CSP、模型源锁定

---

## P1 交互纵深（"能探索" → "能决策"）

- [ ] 从峰到路径：峰 A → 相关峰 B 的学习路径
- [ ] 焦点模式：选中区域淡出其余地形
- [ ] 对比模式：两个时间点 / 两个项目并排
- [ ] 空状态 / 小数据状态：8 / 80 / 800 条各自的长相（现在 demo 1080 掩盖小样本问题）
- [ ] 移动端信息架构：详情 / 时间轴 / 筛选不抢空间
- [ ] 首次打开 30 秒 tour + a11y 补齐（axe 已有依赖，落地 e2e / visual / a11y 测试目录）

---

## P2 增量与工作流闭环（"惊艳一次" → "每周打开"）

- [ ] 增量同步：Obsidian / Notion / 备忘录 / 浏览器收藏，取代每次整包导入
- [ ] 导入后 diff：新增 / 删除 / 改写，并反映在地形上
- [ ] 归档、对比、复盘：学习内容的留存而非一次生成
- [ ] 笔记可编辑：图上改标签、合并重复、钉住重要峰
- [ ] 大样本 perf 门槛 + FPS 分位可观测

---

## 成熟度对照表（发布后按 P2 逐项拉分）

| 维度 | 当前 | P0 后 | P1 后 |
| --- | --- | --- | --- |
| 视觉/空间表达 | 75–85% | 80% | 85% |
| 核心算法骨架 | 70% | 75% | 80% |
| 日常工作流 | 25–35% | 45% | 60% |
| 数据可信与可解释 | 30–40% | 55% | 65% |
| 工程交付/运维 | 20–30% | 55% | 70% |
| 整体"可当产品卖/周活" | 约 40% | 55% | 70% |

---

## 参考对象（plidezus 那类）的三板斧

1. 叙事：打开就有"我的知识如何长成这样"的故事感
2. 社交/分享：一键出一张带品牌、时间、主题的图
3. 个人数据真实感：用"我的笔记"而非合成 demo

---

## 建议执行顺序

1. **P0-1 独立仓 + CI + 部署 URL**（半天）——立刻可"发布"
2. **P0-2 语义可信度**（1–2 天）——免得被用户发现"假语义"
3. **P0-3 项目库 UI**（1–2 天）——产品感立刻上来
4. **P0-4 分享卡片 + 真实数据**（1 天）——专为发帖设计
5. **P0-5 体积拆包**（1–2 天）——线上体验

P0（约 1 周）完成后即可对外发布 vala；P1/P2 逐周滚动。